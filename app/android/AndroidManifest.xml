<?xml version="1.0" encoding="utf-8"?>
<!--
  Aplicación Android del Programa de Desarrollo de Videojuegos Moderno.

  Fíjate en lo que NO hay: ni un solo <uses-permission>. Ni INTERNET.
  El curso entero viaja dentro del APK y se sirve desde los assets, así que la
  app no necesita red para nada — y al no pedir permisos, no hay nada que
  conceder ni nada de lo que fiarse. Es la misma idea que el resto del programa:
  si algo se puede hacer sin pedir acceso, se hace sin pedir acceso.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="io.github.vladimiracunadev.curso"
    android:versionCode="2000"
    android:versionName="2.0.0">

    <uses-sdk android:minSdkVersion="26" android:targetSdkVersion="35" />

    <!-- Para abrir enlaces externos en el navegador del sistema (Android 11+
         exige declarar a qué se consulta; esto NO da acceso a la red). -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
    </queries>

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:supportsRtl="true"
        android:theme="@style/TemaCurso">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode|density|fontScale">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
