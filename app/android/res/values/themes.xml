<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Tema oscuro sin barra de acción: la navegación del curso ya está en la
      propia página. Se fija el color de fondo de la ventana para que al abrir
      no haya un fogonazo blanco antes de que cargue el HTML.
    -->
    <style name="TemaCurso" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/fondo</item>
        <item name="android:statusBarColor">@color/fondo</item>
        <item name="android:navigationBarColor">@color/fondo</item>
        <item name="android:colorAccent">@color/acento</item>
    </style>
</resources>
